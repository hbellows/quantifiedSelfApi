language: node_js
node_js: 
  - "stable"
services:
 - postgresql
before_script:
 - psql -c 'create database quantified_self;' -U postgres
 - knex migrate:lastest
 - knex seed:run
cache:
  directories:
    - "node_modules"
